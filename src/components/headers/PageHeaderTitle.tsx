const PageHeaderTitle = ({ title }: { title: string }): JSX.Element => (
  <h2 className="text-xl font-medium text-slate-700 line-clamp-1 dark:text-navy-50 lg:text-2xl">{title}</h2>
)

export default PageHeaderTitle
